[package]
name = "tool-call-dedup"
version = "0.1.0"
edition = "2021"
description = "Deduplicate repeated identical tool calls in AI agent loops"
license = "MIT"
repository = "https://github.com/MukundaKatta/tool-call-dedup-rs"
keywords = ["agent", "tool", "dedup", "loop", "llm"]
categories = ["api-bindings"]
exclude = ["target/*", ".github/*", "*.log"]

[dependencies]
serde_json = "1"
sha2 = "0.10"
